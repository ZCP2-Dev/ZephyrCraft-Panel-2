{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch (Debug Mode)",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${fileDirname}",
            "env": {
                "DEBUG": "1" // 关键：调试时注入环境变量
            },
            "args": []
        }
    ]
}